/*
    تمرین‌های امروز: کار با توابع (Functions)
    ==========================================

    یه تابع (function) یه تیکه کد جداست که یه کار مشخص انجام میده.
    ساختارش اینجوریه:

        نوع_خروجی نام_تابع(پارامترها) {
            // کد
            return مقدار;
        }

    مثال:
        int doubleIt(int x) {
            return x * 2;
        }

    - "int" اول یعنی این تابع یه عدد صحیح برمی‌گردونه (خروجیش int هست)
    - "doubleIt" اسم تابعه
    - "(int x)" یعنی این تابع یه ورودی (پارامتر) از نوع int میگیره به اسم x
    - "return x * 2;" یعنی خروجی تابع، دو برابر x هست

    توی main() اینجوری صداش می‌زنیم:
        int result = doubleIt(5);   // result میشه 10

    برای هر سوال زیر، امضای تابع (function signature) رو نوشتم،
    فقط باید داخلش رو کامل کنی. هر جا نوشته "کد خودتو اینجا بنویس"
    یعنی اونجا کار توئه. به main() پایین فایل دست نزن.
*/

#include <iostream>
#include <string>
using namespace std;

// -----------------------------------------------------------
// سوال ۱: سال کبیسه
// یه سال میگیره و باید true یا false برگردونه.
// (bool یعنی خروجی فقط یکی از این دوتاست: true یا false)
//
// قانون: اگه سال بر ۴ بخش‌پذیر باشه کبیسه است،
//   مگه اینکه سال قرن باشه (یعنی بر ۱۰۰ هم بخش‌پذیر، مثل ۱۹۰۰)
//   که اون موقع فقط اگه بر ۴۰۰ هم بخش‌پذیر باشه کبیسه است (مثل ۲۰۰۰)
//
// نکته: باقیمونده‌ی تقسیم رو با % به دست میاری، مثلا year % 4
//   اگه باقیمونده صفر بشه یعنی بخش‌پذیره.
// از && (و) ، || (یا) و ! (نقیض) برای ترکیب شرط‌ها استفاده کن.
// -----------------------------------------------------------
bool isLeapYear(int year) {
    // کد خودتو اینجا بنویس

}


// -----------------------------------------------------------
// سوال ۲: بزرگترین عدد
// سه تا عدد میگیره، باید بزرگترینشون رو برگردونه.
//
// نکته: با چند تا if پشت سر هم (یا else if) سه تا رو با هم مقایسه کن.
// -----------------------------------------------------------
int findLargest(int a, int b, int c) {
    // کد خودتو اینجا بنویس

}


// -----------------------------------------------------------
// سوال ۳: سنگ کاغذ قیچی
// انتخاب کاربر رو به صورت رشته (string) میگیره، مثلا "rock"،
// "paper" یا "scissors". انتخاب کامپیوتر همیشه ثابته: "paper"
// باید یکی از این سه تا رو برگردونی: "بردی!" یا "کامپیوتر برد!" یا "مساوی شد!"
//
// نکته: برای مقایسه‌ی دو تا رشته از == استفاده کن:
//   if (userChoice == "rock")
// -----------------------------------------------------------
string playRPS(string userChoice) {
    string computerChoice = "paper";
    // کد خودتو اینجا بنویس

}


// -----------------------------------------------------------
// سوال ۴ (چالش): محاسبه‌ی BMI
// وزن (کیلوگرم) و قد (متر) میگیره، BMI رو حساب می‌کنه و دسته‌بندیش
// رو برمی‌گردونه.
// فرمول: BMI = وزن / (قد × قد)
// دسته‌بندی:
//   کمتر از ۱۸.۵   -> "کم‌وزن"
//   ۱۸.۵ تا ۲۴.۹    -> "نرمال"
//   ۲۵ تا ۲۹.۹      -> "اضافه‌وزن"
//   ۳۰ به بالا      -> "چاق"
//
// نکته: چون قد و وزن عدد اعشاری هستن، به جای int از double استفاده کن.
// -----------------------------------------------------------
string calculateBMI(double weightKg, double heightM) {
    // کد خودتو اینجا بنویس

}


// -----------------------------------------------------------
// از اینجا به پایین دست نزن - این main() از توابع بالا استفاده می‌کنه
// -----------------------------------------------------------
int main() {
    // سوال ۱
    int year;
    cout << "یک سال وارد کن: ";
    cin >> year;
    if (isLeapYear(year))
        cout << year << " کبیسه است." << endl;
    else
        cout << year << " کبیسه نیست." << endl;

    cout << endl;

    // سوال ۲
    int a, b, c;
    cout << "سه تا عدد وارد کن: ";
    cin >> a >> b >> c;
    cout << "بزرگترین عدد: " << findLargest(a, b, c) << endl;

    cout << endl;

    // سوال ۳
    string choice;
    cout << "سنگ، کاغذ یا قیچی رو انتخاب کن (rock/paper/scissors): ";
    cin >> choice;
    cout << playRPS(choice) << endl;

    cout << endl;

    // سوال ۴
    double height, weight;
    cout << "قدت رو به متر وارد کن (مثلا 1.75): ";
    cin >> height;
    cout << "وزنت رو به کیلوگرم وارد کن: ";
    cin >> weight;
    cout << "BMI: " << calculateBMI(weight, height) << endl;

    return 0;
}
